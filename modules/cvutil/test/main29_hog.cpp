/** 
 * @file   main29_hog.cpp
 * @author Liangfu Chen <liangfu.chen@cn.fix8.com>
 * @date   Wed Jun  5 18:01:22 2013
 * 
 * @brief  
 * 
 * 
 */

#include "cvhog.h"

int main()
{
  // CvHOG hog;
  // IplImage * _img = cvLoadImage("../data/hogtrain/raw/00180.png",0);
  // CvMat img_stub;
  // CvMat * img = cvGetMat(_img, &img_stub);
  // int nr=img->rows,nc=img->cols;
  // int csize=6,bound=1;
  // CvMat * hogdesc =
  //     cvCreateMat(1,cvFloor((nc-2*bound)*(nr-2*bound)/csize)*9,CV_32F);
  // CvMat * hogdisp = 
  //     cvCreateMat(nr, nc, CV_32F);
  // hog.extract(img, hogdesc, csize, bound, hogdisp);
  
  return 0;
}
